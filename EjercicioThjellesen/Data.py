class Data(object):
    Altura = []
    Peso = []
    Fecha = []

def setData (self, A, P, F):
    nuevaAltura = Altura()
    nuevaAltura.setAltura(A)
    self.Altura.append(nuevaAltura)

    nuevoPeso = Peso()
    nuevoPeso.setPeso(P)
    self.Peso.append(nuevoPeso)

    nuevaFecha = Fecha()
    nuevaFecha.setFecha(F)
    self.Fecha.append(nuevaFecha)

def buscarFecha (self, FA)
    for item in self.Fecha:
        if item == FA:
            return item

def promedioAltura (self, FA)
    return sum(self.buscarFecha(FA).Altura)/365

def promedioPeso
    return sum(self.buscarFecha(FA).Peso)/365